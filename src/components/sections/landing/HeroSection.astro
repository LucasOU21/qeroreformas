<style>
  .hero-container {
    position: relative;
    z-index: 10;
    min-height: 100vh;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    padding: 3rem 0 0 0; /* Added top padding to create space below navbar */
    margin: 0;
    background-image: url('/images/hero-background.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    overflow: hidden;
  }
  
  @keyframes fadeIn {
    0% { opacity: 0; transform: translateY(20px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  
  .hero-content {
    background: rgba(0, 0, 0, 0.30);
    backdrop-filter: blur(3px);
    padding: 3.5rem 2.5rem;
    border-radius: 12px;
    width: 100%;
    max-width: 40rem;
    margin-left: 10%;
    margin-top: 2rem;
    text-align: center;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.1);
    animation: fadeIn 1s ease-out forwards;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  .brand-name {
    font-family: 'Montserrat', sans-serif;
    font-size: 4rem;
    font-weight: 700;
    letter-spacing: -1px;
    line-height: 1;
    margin-bottom: 0.5rem;
    color: white;
    text-align: center;
  }
  
  .brand-highlight {
    color: #daa520;
    font-weight: 800;
  }
  
  .brand-subtitle {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.25rem;
    font-weight: 300;
    letter-spacing: 2px;
    margin-bottom: 1.5rem;
    color: rgba(255, 255, 255, 0.8);
    text-transform: uppercase;
    text-align: center;
  }
  
  .tagline {
    font-family: 'Inter', sans-serif;
    font-size: 1.1rem;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 2rem;
    max-width: 90%;
    line-height: 1.6;
    white-space: normal;
    word-wrap: break-word;
  }
  
  .cta-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    margin-top: 1rem;
    width: 100%;
    max-width: 400px;
  }
  
  .primary-button {
    background-color: transparent;
    color: #daa520;
    border: 1px solid #daa520;
    border-radius: 8px;
    padding: 0.875rem 2rem;
    font-size: 1rem;
    font-weight: 500;
    letter-spacing: 1px;
    text-transform: uppercase;
    transition: all 0.3s ease;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    max-width: 300px;
  }
  
  .primary-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 0;
    height: 100%;
    background-color: #daa520;
    transition: width 0.3s ease;
    z-index: -1;
  }
  
  .primary-button:hover {
    color: #000;
  }
  
  .primary-button:hover::before {
    width: 100%;
  }
  
  .call-button {
    display: flex;
    flex-direction: column;
    align-items: center;
    color: white;
    font-size: 1.1rem;
    font-weight: 300;
    transition: all 0.3s ease;
    margin-top: 0.5rem;
  }
  
  .call-button svg {
    width: 3rem;
    height: 3rem;
    background: #daa520;
    padding: 0.75rem;
    border-radius: 50%;
    transition: transform 0.3s ease;
    margin-bottom: 0.5rem;
  }
  
  .call-button:hover {
    color: #daa520;
  }
  
  .call-button:hover svg {
    transform: scale(1.1);
  }
  
  @media (max-width: 768px) {
    .hero-container {
      padding-top: 5rem; /* Adjusted for mobile */
    }
    
    .hero-content {
      width: 90%;
      padding: 2.5rem 1.5rem;
      margin: 5rem auto 0;
    }
    
    .brand-name {
      font-size: 3rem;
    }
    
    .brand-subtitle {
      font-size: 1.2rem;
    }
    
    .tagline {
      font-size: 1rem;
    }
  }
</style>

<section class="hero-container">
  <div class="hero-content">
    <h1 class="brand-name">
      <span class="brand-highlight">Kalia</br> Reformas<br />y Decoración
    </h1>
    
    <p class="brand-subtitle">
      Espacios excepcionales
    </p>
    
    <p class="tagline">
      Diseño personalizado • Calidad excepcional • Precio inmejorable
    </p>
    
    <div class="cta-container">
      <a href="/calculadora" class="primary-button">
        CALCULAR PRESUPUESTO
      </a>
      
      <a href="tel:603370840" class="call-button">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
        </svg>
        603 37 08 40
      </a>
    </div>
  </div>
</section>